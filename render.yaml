services:
  - type: web
    name: rag-chatbot
    env: python
    plan: free
    buildCommand: "./render-build.sh"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120"
    envVars:
      - key: HF_TOKEN
        sync: false