services:
  - type: web
    name: rag-chatbot
    env: python
    plan: free
    buildCommand: "./render-build.sh"
    startCommand: "gunicorn --workers 2 --preload app:app --timeout 120"
    envVars:
      - key: HF_TOKEN
        sync: false